# Руководство по адаптивной вёрстке

## Обзор

Этот проект использует **Mobile-First** подход, что означает, что базовые стили написаны для мобильных устройств, а затем расширяются для больших экранов через медиа-запросы.

## Структура брейкпоинтов

### 📱 Mobile (базовые стили)
- **Диапазон:** до 767px
- **Особенности:**
  - Вертикальная навигация (кнопки темы в колонку)
  - Одноколоночная сетка для контента
  - Одноколоночная галерея
  - Минимальная ширина: 375px
  - Отступы: 10px

### 📱 Tablet (768px и выше)
- **Медиа-запрос:** `@media (width >= 768px)`
- **Изменения:**
  - Увеличенные отступы: 20px
  - Увеличенный размер заголовков (clamp для плавного масштабирования)
  - Галерея: 3 колонки с особым расположением изображений
  - Увеличенные промежутки между секциями: 80px
  - Минимальная высота header/footer: 1024px

### 💻 Desktop (1024px и выше)
- **Медиа-запрос:** `@media (width >= 1024px)`
- **Изменения:**
  - Горизонтальная навигация (кнопки темы в ряд)
  - Двухколоночная сетка для карточек контента
  - Трёхколоночная сетка для советов с особым расположением
  - Максимальный размер заголовков (desktop font-size)
  - Улучшенное использование пространства

## Ключевые техники адаптивности

### 1. Использование CSS Custom Properties (переменных)

Переменные определены в `variables.css` и могут быть переопределены в медиа-запросах:

```css
:root {
  --section-padding: 10px; /* Mobile */
}

@media (width >= 768px) {
  :root {
    --section-padding: 20px; /* Tablet/Desktop */
  }
}
```

### 2. Clamp для адаптивной типографики

```css
--font-size-mobile: clamp(7.25rem, 7.0115rem + 1.0178vw, 7.5rem);
--font-size-desktop: clamp(7.5rem, 0.5625rem + 14.4531vw, 9.8125rem);
```

Это обеспечивает плавное масштабирование шрифтов между брейкпоинтами.

### 3. CSS Grid для адаптивных сеток

**Mobile (1 колонка):**
```css
.content__grid {
  display: grid;
  gap: var(--gap);
}
```

**Desktop (2 колонки):**
```css
@media (width >= 1024px) {
  .content__grid {
    grid-template-columns: repeat(2, 1fr);
  }
}
```

### 4. Специальные макеты для галереи

**Tablet/Desktop - мозаичный layout:**
```css
.gallery__grid {
  grid-template-columns: repeat(3, 1fr);
}

.gallery__image:nth-child(1) {
  grid-column: span 2; /* Первое изображение занимает 2 колонки */
}

.gallery__image:nth-child(2) {
  grid-row: span 2; /* Второе изображение занимает 2 ряда */
}
```

## Как добавить новые адаптивные стили

### Шаг 1: Определите брейкпоинт

Используйте существующие брейкпоинты:
- **768px** для планшетов
- **1024px** для десктопов

Или добавьте новый:
```css
@media (width >= 1440px) {
  /* Стили для больших экранов */
}
```

### Шаг 2: Добавьте стили в соответствующий медиа-запрос

В файле `styles/style.css` найдите нужный медиа-запрос и добавьте стили:

```css
@media (width >= 1024px) {
  .your-element {
    /* Ваши стили для десктопа */
  }
}
```

### Шаг 3: Используйте логические свойства

Для лучшей поддержки RTL (right-to-left) языков используйте:
- `inline-size` вместо `width`
- `block-size` вместо `height`
- `inset-inline-start` вместо `left`
- `inset-inline-end` вместо `right`
- `margin-inline-start` вместо `margin-left`

## Примеры адаптивных паттернов

### Адаптивная навигация

**Mobile:** Вертикальная
```css
.header__theme-menu-list {
  flex-direction: column;
}
```

**Desktop:** Горизонтальная
```css
@media (width >= 1024px) {
  .header__theme-menu-list {
    flex-direction: row;
  }
}
```

### Адаптивные отступы

**Mobile:**
```css
.content {
  padding: 40px 10px;
}
```

**Tablet/Desktop:**
```css
@media (width >= 768px) {
  .content {
    padding: 60px 20px;
  }
}
```

### Адаптивная сетка контента

**Mobile:** 1 колонка
```css
.content__grid {
  display: grid;
  gap: 20px;
}
```

**Desktop:** 2 колонки
```css
@media (width >= 1024px) {
  .content__grid {
    grid-template-columns: repeat(2, 1fr);
  }
}
```

## Тестирование адаптивности

### Инструменты для тестирования:

1. **Chrome DevTools:**
   - F12 → Toggle Device Toolbar (Ctrl+Shift+M)
   - Выберите устройства или задайте кастомные размеры

2. **Рекомендуемые размеры для тестирования:**
   - Mobile: 375px, 414px, 428px
   - Tablet: 768px, 834px, 1024px
   - Desktop: 1280px, 1440px, 1920px

3. **Проверьте:**
   - ✅ Нет горизонтального скролла
   - ✅ Текст читаем на всех размерах
   - ✅ Изображения не переполняют контейнеры
   - ✅ Интерактивные элементы доступны (достаточный размер для клика)
   - ✅ Отступы и промежутки выглядят сбалансированно

## Советы по оптимизации

1. **Используйте `min-width` вместо `max-width`** в медиа-запросах (Mobile-First)
2. **Избегайте фиксированных размеров** - используйте относительные единицы (%, rem, em, vw, vh)
3. **Тестируйте на реальных устройствах**, а не только в DevTools
4. **Оптимизируйте изображения** для разных размеров экрана (используйте `srcset` и `sizes`)
5. **Используйте `loading="lazy"`** для изображений ниже сгиба страницы

## Текущая структура медиа-запросов

```css
/* Базовые стили (Mobile) */
/* ... */

/* Tablet: 768px+ */
@media (width >= 768px) {
  /* ... */
}

/* Desktop: 1024px+ */
@media (width >= 1024px) {
  /* ... */
}
```

## Дополнительные ресурсы

- [MDN: Responsive Design](https://developer.mozilla.org/en-US/docs/Learn/CSS/CSS_layout/Responsive_Design)
- [CSS Logical Properties](https://developer.mozilla.org/en-US/docs/Web/CSS/CSS_Logical_Properties)
- [CSS Grid Guide](https://css-tricks.com/snippets/css/complete-guide-grid/)

